group 'com.epam.esm'
version '1.0'

allprojects {
    apply plugin: 'java'

    ext {
        springVersion = '5.2.12.RELEASE'
    }

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.12'
    }
}
